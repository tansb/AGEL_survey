# Pip-only fallback — conda (environment.yml) is strongly preferred.
#
# To install: pip install -r requirements.txt

# Core scientific stack
numpy>=1.23
pandas>=1.5
requests>=2.28
matplotlib>=3.6

# Astropy ecosystem
astropy>=5.2
astroquery>=0.4.6   # MAST (JWST) and HEASARC (Chandra/XRISM/XMM/…) queries
reproject>=0.9      # WCS reprojection for RGB channel alignment
